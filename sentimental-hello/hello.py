# Prompt the user for their name and save the input to a variable called "name"
name = input("What is your name?\n")

# Build a personalized greeting by concatenating the string "hello, " with the user's name
greeting = "hello, " + name

# Print the personalized greeting to the console
print(greeting)
